<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">

        <% include ../partials/head %>

    </head>
    <body>

        <% include ../partials/header %>

        <form method="post" action="/predictions">
            <input type="hidden" name="matchDay" value="<%=data[0].season.currentMatchday%>">
            <fieldset>
                <legend><h1>PREDICTIONS</h1></legend>
                <table>
                    <tr>
                        <th>Home Win</th>
                        <th>Draw</th>
                        <th>Away Win</th>
                    </tr>

                    <% for (let i = 0; i < data.length; i++) { %>

                    <tr>
                        <td><label><input value="HOME_WIN" type="radio" name="fixture_<%=i+1%>" required><%= data[i].homeTeam.name %></label><input type="hidden" name="fixtureHome_<%=i+1%>" value="<%= data[i].homeTeam.name %>"></td>
                        <td class="thin-col"><input value="DRAW" type="radio" name="fixture_<%=i+1%>">
                        <td><label><%= data[i].awayTeam.name %><input value="AWAY_WIN" type="radio" name="fixture_<%=i+1%>"></label><input type="hidden" name="fixtureAway_<%=i+1%>" value="<%= data[i].awayTeam.name %>"></td>
                    </tr>

                    <% } %>

                </table>
            </fieldset>
            <h2>If you have already submitted predictions for this week you will be redirected to the update predictions page when you hit the submit button</h2>
            <button type="submit">Submit Predictions</button>
        </form>

        <a class="button" href="/leaderboard">Check Leaderboard</a>
        <a class="button" href="/results">Check Results</a>
        <a class="button" href="/profile">Home Page</a>

        <% include ../partials/footer %>

    </body>
</html>
